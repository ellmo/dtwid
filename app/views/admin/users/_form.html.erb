
  <%= simple_form_for [:admin, @user] do |f| %>
    <%= f.input :nick, :required => true %>
    <%= f.input :team, :as => :boolean %>
    <%= f.button :submit, :class => "submit_button" %>
  <% end %>

