<%= form_for(@map_episode) do |f| %>
  <% if @map_episode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@map_episode.errors.count, "error") %> prohibited this map_episode from being saved:</h2>

      <ul>
      <% @map_episode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
