<%= form_for(@map_slot) do |f| %>
  <% if @map_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@map_slot.errors.count, "error") %> prohibited this map_slot from being saved:</h2>

      <ul>
      <% @map_slot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
