<h1>Listing submission_comments</h1>

<table>
  <tr>
    <th>Submission</th>
    <th>User</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @submission_comments.each do |submission_comment| %>
  <tr>
    <td><%= submission_comment.submission_id %></td>
    <td><%= submission_comment.user_id %></td>
    <td><%= submission_comment.comment %></td>
    <td><%= link_to 'Show', submission_comment %></td>
    <td><%= link_to 'Edit', edit_submission_comment_path(submission_comment) %></td>
    <td><%= link_to 'Destroy', submission_comment, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Submission comment', new_submission_comment_path %>
